<p id="notice"><%= notice %></p>
<div class="jumbotron">
<p>
  <strong>Question:</strong>
  <%= @question.name %>
</p>

</div>
</br>

<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


<p id="notice"><%= notice %></p>

<div class="jumbotron">
<h1>Answers</h1>

<table class="table">
  <thead>
    <tr>
      <th>Content</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= post.content %></td>
        <td><%= link_to 'Edit', edit_post_path(post),class:" btn btn-success"%></td>
        <td><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } ,class:"btn btn-danger"%></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>

<br>

    
     <h3>Comments:-</h3><br>
     <div class="jumbotron">
    <%= f.label :content %><br>
    <%= f.text_area :content %><br>
   
   <input type="hidden" name="post[question_id]" value="<%= @question.id %>" />

 
    <%= f.submit %>
</div>
 <br>

<% end %>
<br>
<%= link_to 'Edit', edit_question_path(@question),class:"btn btn-primary" %> |
<% @forum.each do |x| %>
<%= link_to "Back", {controller: "forums", action: "show", id: x.id, class:" btn btn-info"}%>
<% end %>


